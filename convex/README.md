# SSHThing Convex Backend

This package will host:

- Clerk-backed auth integration
- Teams schema
- CLI auth session issuance and polling
- Team hosts and members APIs
- Shared credential storage and connect grants

Environment variables and setup instructions are documented in `docs/teams/SETUP.md`.

